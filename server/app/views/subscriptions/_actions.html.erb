<%= render 'partials/row_actions' do %>
  <li>
    <%= link_to 'Edit', edit_subscription_path(subscription), {:remote => true, 'data-toggle' =>  "modal", 'data-target' => '#modal-window'}%>
  </li>
  <li class="divider"></li>
  <li>
    <%= link_to 'Delete', subscription, :remote => true, method: :delete, data: { confirm: 'Are you sure?' } %>
  </li>
<% end %>
